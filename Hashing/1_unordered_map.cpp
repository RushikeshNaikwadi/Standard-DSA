#include <bits/stdc++.h>
using namespace std;

template <typename T>
struct keyValNode {
    string key;
    T val;
    keyValNode *next;

    keyValNode (string inpKey, T inpVal) {
        key = inpKey;
        val = inpVal;
        next = NULL;
    }
};

int main () {

    return 0;
}